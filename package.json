{
  "name": "kikanko",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "VITE_BASE=/kikanko/ npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "puppeteer": "^24.36.0",
    "terser": "^5.31.0",
    "vite": "^5.4.0"
  }
}
